{
  "name": "english-grammar-master",
  "version": "2.0.0",
  "description": "English Grammar Master - PWA with offline support and modular architecture",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate-icons": "node generate-icons.cjs",
    "lint": "eslint js/**/*.js",
    "format": "prettier --write 'js/**/*.js' '*.html' '*.css'"
  },
  "dependencies": {
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "eslint": "^8.56.0",
    "prettier": "^3.1.0"
  }
}
